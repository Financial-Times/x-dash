import { h } from '@financial-times/x-engine';
import styles from './GiftArticle.css';

export default () => (
	<div class={ styles['loading-spinner__container'] }>
		<div class="o-loading o-loading--dark o-loading--large"></div>
	</div>
);
